// Placeholder de la página del directorio para reservar la ruta pública inicial.
export default function DirectorioPage() {
  return <div>DirectorioPage — pendiente de implementación (M1/M2/M3...)</div>
}
