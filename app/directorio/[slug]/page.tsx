// Placeholder de detalle de perfil por slug para reservar la ruta dinámica del directorio.
export default function DirectorioSlugPage() {
  return (
    <div>DirectorioSlugPage — pendiente de implementación (M1/M2/M3...)</div>
  )
}
