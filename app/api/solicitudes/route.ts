// Placeholder de API de solicitudes para definir el contrato base de la ruta.
import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({ message: 'ruta pendiente de implementación' })
}
