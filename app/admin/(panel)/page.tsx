// Placeholder de home del panel admin para reservar la ruta base administrativa.
export default function AdminPanelPage() {
  return <div>AdminPanelPage — pendiente de implementación (M1/M2/M3...)</div>
}
