// Placeholder de login admin para reservar el acceso al panel administrativo.
export default function AdminLoginPage() {
  return <div>AdminLoginPage — pendiente de implementación (M1/M2/M3...)</div>
}
